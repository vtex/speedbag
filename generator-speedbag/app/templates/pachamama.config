[
	{
		"product": "<%= _.slugify(appname) %>",
		"acronym": "<%= _.slugify(appname).substring(0,6) %>",
		"layer": "frontend",
		"application": "<%= _.slugify(appname) %>",
		"roots": [],
		"integration": {
            "javascript": {
                "npm": ["install"],
                "grunt": ["dist test --no-color --stack"]
            }
        },
        "infrastructure": {
            "javascript": {

                "grunt": ["vtex_deploy --no-color --stack"]
            }
        }
	}
]